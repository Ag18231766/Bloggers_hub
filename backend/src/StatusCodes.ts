enum StatusCodes {
    OK = 200,
    CREATED = 201,
    NOT_FOUND = 404,
    CONFLICT = 409,
    SERVICE_UNAVAILABLE = 503,
    BAD_GATEWAY = 502,
    FORBIDDEN = 403,
    BADREQUEST = 400
}

export default StatusCodes;